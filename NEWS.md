# SQLVault version 0.1.1

### Improvements

- The function `release_project()` has a new argument `version_table` to provide
  a table recording version's information.

### Bug fixes

- Functions `init_project()` and `release_project()` close opened connections
  on exit.


# SQLVault version 0.1.0

### New features

- First release
